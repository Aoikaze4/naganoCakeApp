<div class="container">
  <div class="row">
    <div class="col-md-4">
      <%= attachment_image_tag @item, :image, size: "300x300", fallbaxk: "no_image.jpg" %>
    </div>
    <div class="col-md-8">
      <div class="col-md-12">
        <h2><%= @item.name %></h2>
        <div class="col-md-12">
          <p><%= @item.introduction %></p>
        </div>
        <div class="col-md-12">
          <p><%= @genre.name %></p>
        </div>
        <div class="col-md-12">
          税込み価格(税抜き価格)
          <%= add_tax_price.to_s(:delimited, delimiter: ',') %>
          <span>(</span><%= @item.price.to_s(:delimited, delimiter: ',') %><span>) 円</span>
          <span class="add_tax"></span></h3>
        </div>
        <div class="col-md-12">
          <p>販売ステータス</p>
          <% if @item.is_active == true %>
            <p class="text-success">販売中</p>
          <% else %>
            <p class="text-danger">販売停止中</p>
          <% end %>
        </div>
        <div class="col-md-12">
          <%= link_to "編集する", edit_admin_item_path, class: "btn btn-success" %>
        </div>
      </div>
    </div>
  </div>
</div>