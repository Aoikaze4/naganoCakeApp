<div class="container">
  <div class="row">
    <div class="mx-auto p-5">
      <p>ようこそ、ながのCAKEへ！<br />
      このサイトは、ケーキ販売のECサイトになります。<br />
      会員でない方も商品の閲覧はできますが、<br />
      購入には会員登録が必要になります。<br /></p>
    </div>
  </div>

  <h3 class="m-3">新着商品</h3>
  <div class="row justify-content-start">
    <% @items.each do |f| %>
      <div class="col-3">
        <div>
          <%= link_to item_path(f.id) do %>
          <%= attachment_image_tag f, :image, size: "120x120", fallback: "no_image.jpg",style: "width: 160px; height: 160px;", class: "img-fluid mx-3"%>
          <% end %>
        </div>
        <div>
          <p class="my-1"><%= link_to f.name, item_path(f.id) %></p>
        </div>
        <div>
          <p>￥ <%= add_tax_price(f.price).to_s(:delimited, delimiter: ',') %></p>
        </div>
      </div>
      <% end %>
    </div>
    <%= link_to "全ての商品を見る >", items_path, class: "my-1 float-right" %>
  </div>
</div>